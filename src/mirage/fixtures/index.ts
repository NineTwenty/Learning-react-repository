import usersFixture from './users';
import dialogsFixture from './dialogs';
import messagesFixture from './messages';
import postsFixture from './posts';
import feedsFixture from './feeds';

export default {
  users: usersFixture,
  dialogs: dialogsFixture,
  messages: messagesFixture,
  posts: postsFixture,
  feeds: feedsFixture,
};
